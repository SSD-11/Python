# Calcular el consumo promedio de un coche cuando se conoce la distancia total recorrida (en km) y el total
# de combustible gastado (en litros).

x = int(input())
y = float(input())
print("{:.3f} km/l".format(x / y))
